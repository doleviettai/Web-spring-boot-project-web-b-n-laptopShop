spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto : update
  datasource:
    url : "jdbc:mysql://localhost:3306/hellospring"
    username : root
    password : taido231105
#  security:
#    user:
#     name: admin@gmail.com
#     password: "$2a$10$M1XUlm3Kg9JyPcYM9AxBn.9WMLR2OJX3w7tC8u2WQiKECIwx9mbmu" #123
  mvc :
    view :
      prefix: /WEB-INF/view/
      suffix: .jsp
  servlet :
    multipart :
      max-file-size : 50MB
      max-request-size : 50MB
  session:
    store-type: jdbc
    timeout: 30m
    jdbc:
      initialize-schema: always

logging:
  level:
    org:
      springframework:
        security : DEBUG

#        spring.session.store-type=jdbc
#        spring.session.timeout=30m
#        spring.session.jdbc.initialize-schema=always
#        server.servlet.session.timeout
#        spring.session.jdbc.table-name=SPRING_SESSION